# Copy the right one of .env.development / .env.staging / .env.production to
# .env before running/building admin_panel for that target (flutter_dotenv
# loads a fixed ".env" filename, bundled as an asset — see pubspec.yaml).
API_BASE_URL=
